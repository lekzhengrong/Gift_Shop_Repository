<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Step 3 Layout</title>
    <style>
        body {
            margin: 0;
            font-family: Arial, sans-serif;
        }

        .container {
            display: flex;
            justify-content: space-between;
            padding: 0 12.5%; /* 12.5% empty space on left and right */
        }

        .content {
            width: 100%;
            max-width: 75%; /* Adjust content width */
        }

        .sticky-cart {
            position: sticky;
            top: 20px;
            width: 20%;
            max-width: 300px;
            height: auto;
            background-color: #f8f8f8;
            border: 1px solid #ddd;
            border-radius: 8px;
            padding: 16px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .sticky-cart h3 {
            margin: 0;
            margin-bottom: 16px;
            font-size: 1.2em;
        }

        .sticky-cart button {
            width: 100%;
            padding: 10px;
            background-color: #007bff;
            color: white;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        .sticky-cart button:hover {
            background-color: #0056b3;
        }

        .content-section {
            margin-bottom: 20px;
        }

        .content h1 {
            font-size: 2em;
            margin-bottom: 16px;
        }

        .content p {
            font-size: 1em;
            line-height: 1.6;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="content">
            <h1>Step 3: Content Area</h1>
            <div class="content-section">
                <p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Nulla scelerisque turpis a justo cursus, non laoreet lacus pretium.</p>
            </div>
            <div class="content-section">
                <p>More information or descriptions for Step 3 go here. Add any additional sections as needed.</p>
            </div>
        </div>
        <div class="sticky-cart">
            <h3>Your Cart</h3>
            <ul>
                <li>Item 1</li>
                <li>Item 2</li>
                <li>Item 3</li>
            </ul>
            <button>Checkout</button>
        </div>
    </div>

    <script>
        const overlay = document.getElementById('popup-overlay');
        const closeBtn = document.getElementById('popup-close');
        const saveBtn = document.getElementById('save-button');
        let selectedProductId = null;

        document.querySelectorAll('.select-design').forEach(button => {
            button.addEventListener('click', function() {
                selectedProductId = this.getAttribute('data-product-id');
                overlay.style.display = 'flex';
            });
        });

        closeBtn.addEventListener('click', function() {
            overlay.style.display = 'none';
        });

        saveBtn.addEventListener('click', function() {
            const message = document.getElementById('gift-message').value;
            const fromName = document.getElementById('gift-from').value;

            if (!message || !fromName) {
                alert('Please fill out both the message and the "From" field.');
                return;
            }

            // Save the data to the backend
            fetch('/cart/update.js', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                },
                body: JSON.stringify({
                    updates: {
                        [selectedProductId]: 1
                    },
                    note: `Message: ${message}, From: ${fromName}`
                })
            }).then(response => {
                if (response.ok) {
                    alert('Gift card customization saved!');
                    overlay.style.display = 'none';
                } else {
                    alert('Failed to save the customization. Please try again.');
                }
            });
        });
    </script>
</body>
</html>
